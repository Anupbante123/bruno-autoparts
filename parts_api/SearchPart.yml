info:
  name: SearchPart
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/parts/v1/searchPart?searchPartNumber=8a
  params:
    - name: searchPartNumber
      value: 8a
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
