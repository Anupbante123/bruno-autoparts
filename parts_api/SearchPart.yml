info:
  name: SearchPart
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/parts/searchPart?searchPartNumber=23
  params:
    - name: searchPartNumber
      value: "23"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
